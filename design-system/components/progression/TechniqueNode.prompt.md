A purchasable technique in a discipline tree. Grid is three columns.

```jsx
<TechniqueNode tier="Adept" name="Measured Draw" state="available" cost="1 point"
  description="Holding a shot past its window trades speed for penetration." />
```

Dim locked nodes to 55%; never hide them. When an exclusive pair is resolved, the sibling becomes `locked`.
