Circular sigil identifying the speaker.

```jsx
<SpeakerMark initial="O" />
```

Deliberately a letter rather than a portrait — keeps every speaker consistent while character art is incomplete.

## Accessibility

It is decorative by default because the adjacent heading names the speaker. Set `decorative={false}` and provide `label` only when it stands alone.
