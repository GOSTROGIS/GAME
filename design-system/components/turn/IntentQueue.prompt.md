Ordered list of enemy intents in the quest-tracker anchor.

```jsx
<IntentQueue intents={intents} selectedId="intent.ash-lunge" onInspect={inspect} />
```

Order follows authoritative resolution bands and stable actor ordering; the
client never estimates it from animation.

## Accessibility

Use the native ordered list, expose the declared count, and preserve each
`IntentTelegraph` summary. Keyboard inspection follows DOM order.
