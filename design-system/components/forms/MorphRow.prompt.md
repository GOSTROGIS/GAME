A single appearance morph slider. Sixteen of these drive the creator.

```jsx
<MorphRow id="cheek" label="Cheek hollow" value={v} onChange={setV} />
```

Serif labels, gold accent. Changes should drive the live morph profile, which must stay visibly labelled as a prototype rig.
