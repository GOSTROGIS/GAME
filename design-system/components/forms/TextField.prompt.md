Text input for names and player-authored strings.

```jsx
<FieldLabel htmlFor="name">Pilgrim's Name</FieldLabel>
<TextField id="name" value={name} onChange={setName} />
```

Serif at 19px is deliberate — the player is writing into the world. Don't switch it to sans.
