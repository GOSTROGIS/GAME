Increment control for attribute points.

```jsx
<StatStepper value={vigor} min={8} max={20} onChange={setVigor} />
```

Always accompanied by a BudgetBar. Disable at the bounds rather than clamping silently.
