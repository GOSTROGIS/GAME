Edge darkening for any full-bleed scene.

```jsx
<Vignette />
```

One per scene, always with Grain. Requires a positioned parent.

## Accessibility

The layer is `aria-hidden` and pointer-transparent. Verify that it does not reduce required text contrast.
